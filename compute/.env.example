# Local dev:
DATABASE_URL=postgresql://localhost/ccpo_dev
# Supabase (service role — server side only, never in a browser):
# DATABASE_URL=postgresql://postgres.<ref>:<password>@aws-0-ap-south-1.pooler.supabase.com:5432/postgres

